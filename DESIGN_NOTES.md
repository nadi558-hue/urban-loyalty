# הערות עיצוב ובאגים — Urban Loyalty
תאריך: 2026-07-07 · נבדק על localhost:3000, מסך התחברות ועד דשבורד

## 1. באגים לתיקון (עדיפות גבוהה)

### 1.1 כפתור "שלח קוד אימות" מנוטרל בלי הסבר
קובץ: `app/(auth)/login/page.tsx` שורה 127
הכפתור `disabled={loading || phone.length < 9 || !agreed}` — כל עוד תיבת הסימון "קראתי ואני מסכים/ה לתקנון" לא מסומנת, הכפתור נשאר disabled בלי שום משוב למשתמש. יש כבר הודעת שגיאה מוכנה בקוד (שורה 26: `'יש לאשר את תקנון ותנאי השימוש כדי להמשיך'`) אבל היא אף פעם לא מוצגת כי אי אפשר בכלל ללחוץ על כפתור מנוטרל.
**תיקון מוצע:** הסר את `!agreed` מה-disabled, ותן ל-`sendOtp()` להציג את הודעת השגיאה בפועל כשלוחצים בלי לאשר. כך המשתמש מקבל פידבק במקום להרגיש שהאפליקציה תקועה.

### 1.2 כפתורי "מימוש" בחנות ההטבות לא מחוברים
קובץ: `app/(member)/rewards/page.tsx` שורה 155
הכפתור הוא `<button style={...}>מימוש</button>` בלי `onClick` בכלל. יש כבר endpoint מוכן ב-`app/api/rewards/redeem/route.ts` שלא בשימוש.
**תיקון מוצע:** לחבר את הכפתור ל-POST לאותו endpoint + מודאל אישור לפני מימוש (יש UC 47 זמין, לכל כרטיס יש עלות ב-UC — כדאי גם לנטרל/להסתיר כפתור מימוש לפריטים שעולים יותר מהיתרה הזמינה).

### 1.3 רשימת ההיסטוריה לא ממוינת לפי תאריך
קובץ: `app/(member)/history/page.tsx` שורות 15-19
הנתונים (כרגע mock) מסודרים לפי סדר יצירה במערך ולא לפי `created_at` — בתצוגה זה יוצא: 5 ביולי, 6 ביולי, 7 ביולי, 2 ביולי, 27 ביוני (לא כרונולוגי).
**תיקון מוצע:** למיין את הרשימה לפי `created_at` יורד (חדש→ישן) לפני הרינדור, ולוודא שכשמתחברים למקור נתונים אמיתי יש `ORDER BY created_at DESC` בשאילתה.

### 1.4 חוסר עקביות בנתוני משתמש בין מסכים
מסך הבית מציג את השם האמיתי של המשתמש המחובר ("נדב בנימין"), אבל מסך הפרופיל ומסך כרטיס ה-QR מציגים שם קבוע-מקודד ("מאיה לוי", מספר חבר URB-2847). כנראה רק דף הבית מחובר לנתוני האימות בפועל, ושאר המסכים עדיין על mock.
**תיקון מוצע:** לחבר את `app/(member)/profile/page.tsx` ואת מסך ה-QR (`/qr`) לאותו מקור נתונים כמו דף הבית.

## 2. הערות עיצוב/UX

### 2.1 מסך התחברות — קונטרסט נמוך
- תיבת הסימון "מסכים/ה לתקנון" כמעט בלתי נראית על גבי תמונת הרקע העמוסה (border דק ברמת שקיפות 0.25) — זו הסיבה שבפועל פספסתי אותה בבדיקה. מומלץ רקע אטום קטן מאחורי כל השורה, לא רק מסביב לתיבה עצמה.
- שדה הטלפון (placeholder אפור בהיר על overlay שקוף) עלול להיות קשה לקריאה בשמש. מומלץ רקע אטום יותר.
- אין תווית קבועה מעל השדה — רק placeholder שנעלם בהקלדה. כדאי תווית טקסט קטנה קבועה.
- שקול קוד מדינה מפורש (+972) ליד השדה.

### 2.2 כפתור QR מרכזי בתפריט התחתון
הכפתור המרכזי בעיצוב בולט (עיגול זהב) מוביל למסך "כרטיס חבר" (QR) — לוגי כרעיון (כניסה מהירה לסטודיו), אבל אין לו תווית טקסט כמו שאר האייקונים (פרופיל/היסטוריה/הטבות/בית). כדאי תווית קטנה גם כאן לעקביות ולנגישות.

### 2.3 מה שעובד טוב
- זהות ויזואלית עקבית: שחור/זהב, טיפוגרפיה מוקצנת, תמונות שחור-לבן מהסטודיו — מרגיש בוטיק ולא גנרי.
- מד ה-UC העגול בדף הבית + פס ההתקדמות ל-Gold ברור ומניע.
- כרטיסי "אתגרים פעילים" עם progress bar (חודש מלא, רצף שיעורים) — גיימיפיקציה טובה שמעודדת חזרה לאפליקציה.
- חנות ההטבות: הכרטיס המומלץ מציג בדיוק כמה UC חסרים ("חסרים UC 63") — עידוד ברור לצבור עוד.
- קידוד צבעים בהיסטוריה (ירוק/זהב לצבירה, אדום למימוש) — קריא ומיידי.

## 3. סיכום עדיפויות
1. תקן את disabled על כפתור ההתחברות (1.1) — חוסם כניסה לגמרי כרגע.
2. חבר את כפתורי המימוש ל-API הקיים (1.2).
3. מיין את ההיסטוריה לפי תאריך (1.3).
4. אחד את מקור נתוני המשתמש בין המסכים (1.4).
5. שפר קונטרסט במסך ההתחברות (2.1).
6. הוסף תווית טקסט לאייקון ה-QR המרכזי (2.2).
